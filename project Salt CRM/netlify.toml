[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback : toutes les routes renvoient index.html (react-router gère le reste)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
